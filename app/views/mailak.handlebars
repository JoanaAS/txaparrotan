{{> menuadmin}}

<div class="jumbotron">
        <h2>Mailak</h2>
      </div>

     <div class="container">

<div class="row marketing">

        <div class="col-lg-10">
          <div class="col-lg-2">
          <form class="form-horizontal newsletterForm" role="form" action="/admin/mailakgehitu" method="POST">
        <input type="hidden" name="_csrf" value="{{_csrfToken}}">
          </div>
         <div class="form-group">
            <div class="col-sm-offset-3"> 
                <button type="submit" class="btn btn-default">Gehitu</button>
            </div>
        </div>
        </form> 
            <table class="table table-striped">
            <tr>
                    <th>Izena</th>
                    <th>Akronimoa</th>
                    <th>Maila zkia</th>
                    <th>Multzo kopurua</th>
                    <th>Ordutegia zkia</th>
                    <th>Finalak</th>
                    <th></th>
                    <th></th>

                </tr>                               
            {{#each data}}
                  <tr>
                    <td>{{mailaizena}}</td>
                    <td>{{akronimoa}}</td>
                    <td>{{mailazki}}</td>
                    <td>{{multzokop}}</td>
                    <td>{{ordutegiazki}}</td>
                    <td>{{finalak}}</td>
                     <td>
                        <a class="a-inside edit" href="../admin/mailakeditatu/{{idmaila}}">Aldatu</a>
                    </td>
                    <td>
                        <a class="a-inside delete" href="../admin/mailakezabatu/{{idmaila}}">Ezabatu</a> 
                    </td>
                    
                </tr>
            {{/each}}
          </table>
        </div>

        
      </div>
      
</div>
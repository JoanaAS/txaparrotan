{{> menuadmin}}

<div class="jumbotron">
        <h2>Zelaiak</h2>
      </div>

     <div class="container">

<div class="row marketing">

        <div class="col-lg-6">
          <div class="col-lg-2">
          <form class="form-horizontal newsletterForm" role="form" action="/admin/zelaiakgehitu" method="POST">
        <input type="hidden" name="_csrf" value="{{_csrfToken}}">
          </div>
         <div class="form-group">
            <div class="col-sm-offset-3"> 
                <button type="submit" class="btn btn-default">Gehitu</button>
            </div>
        </div>
        </form> 
            <table class="table table-striped">
            <tr>
                    <th>Izena</th>
                    <th>Zenbagarren zelaia</th>
                    <th></th>
                    <th></th>

                </tr>                               
            {{#each data}}
                  <tr>
                    <td>{{zelaiizena}}</td>
                    <td>{{zelaizki}}</td>
                     <td>
                        <a class="a-inside edit" href="../admin/zelaiakeditatu/{{idzelaia}}">Aldatu</a>
                    </td>
                    <td>
                        <a class="a-inside delete" href="../admin/zelaiakezabatu/{{idzelaia}}">Ezabatu</a> 
                    </td>
                    
                </tr>
            {{/each}}
          </table>
        </div>

        
      </div>
      
</div>